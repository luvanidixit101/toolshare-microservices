server:
  port: 8082

spring:
  application:
    name: user-service
<<<<<<< HEAD

  config:
    import: optional:configserver:http://localhost:8888

  datasource:
    url: jdbc:postgresql://localhost:5432/toolshare_user
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
=======
  datasource:
    url: jdbc:postgresql://${DB_HOST:${POSTGRES_HOST:localhost}}:${DB_PORT:${POSTGRES_PORT:5432}}/${USER_DB_NAME:toolshare_user}
    username: ${DB_USERNAME:${POSTGRES_USER:admin}}
    password: ${DB_PASSWORD:${POSTGRES_PASSWORD:admin}}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  jackson:
    default-property-inclusion: non_null

toolshare:
  security:
    jwt-secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)

management:
  endpoints:
    web:
      exposure:
        include: health,info
<<<<<<< HEAD

instance:
  prefer-ip-address: true
=======
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.toolshare.user: INFO

>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)
