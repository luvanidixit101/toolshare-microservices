<<<<<<< HEAD
server:
  port: 8081


spring:

  application:
    name: auth-service


  config:
    import: optional:configserver:http://localhost:8888


  datasource:
    url: jdbc:postgresql://localhost:5432/toolshare_auth
    username: postgres
    password: admin

=======
# server:
#   port: 8081

# spring:
#   application:
#     name: auth-service
#   datasource:
#     url: jdbc:postgresql://${DB_HOST:${POSTGRES_HOST:localhost}}:${DB_PORT:${POSTGRES_PORT:5432}}/${AUTH_DB_NAME:toolshare_auth}
#     username: ${DB_USERNAME:${POSTGRES_USER:admin}}
#     password: ${DB_PASSWORD:${POSTGRES_PASSWORD:admin}}
#   jpa:
#     hibernate:
#       ddl-auto: update
#     open-in-view: false
#     properties:
#       hibernate:
#         format_sql: true
#   jackson:
#     default-property-inclusion: non_null

# toolshare:
#   security:
#     jwt-secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
#     access-token-minutes: ${JWT_ACCESS_TOKEN_MINUTES:60}
#     refresh-token-days: ${JWT_REFRESH_TOKEN_DAYS:7}
#   frontend-url: ${FRONTEND_URL:http://localhost:5173}

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,info
#   endpoint:
#     health:
#       probes:
#         enabled: true

# springdoc:
#   swagger-ui:
#     path: /swagger-ui.html

# logging:
#   level:
#     com.toolshare.auth: INFO

server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:${POSTGRES_HOST:localhost}}:${DB_PORT:${POSTGRES_PORT:5432}}/${AUTH_DB_NAME:toolshare_auth}
    username: ${DB_USERNAME:${POSTGRES_USER:admin}}
    password: ${DB_PASSWORD:${POSTGRES_PASSWORD:admin}}
>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)

  jpa:
    hibernate:
      ddl-auto: update
<<<<<<< HEAD

    show-sql: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

    register-with-eureka: true
    fetch-registry: true

management:

=======
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  jackson:
    default-property-inclusion: non_null

toolshare:
  security:
    jwt-secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
    access-token-minutes: ${JWT_ACCESS_TOKEN_MINUTES:60}
    refresh-token-days: ${JWT_REFRESH_TOKEN_DAYS:7}
    frontend-url: ${FRONTEND_URL:http://localhost:5173}

management:
>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)
  endpoints:
    web:
      exposure:
        include: health,info
<<<<<<< HEAD
  

instance:
  prefer-ip-address: true
=======

  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.toolshare.auth: INFO
>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)
