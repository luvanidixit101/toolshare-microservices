server:
  port: 8084

spring:
  application:
    name: booking-service
<<<<<<< HEAD

  config:
    import: optional:configserver:http://localhost:8888

  datasource:
    url: jdbc:postgresql://localhost:5432/toolshare_booking
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
=======
  datasource:
    url: jdbc:postgresql://${DB_HOST:${POSTGRES_HOST:localhost}}:${DB_PORT:${POSTGRES_PORT:5432}}/${BOOKING_DB_NAME:toolshare_booking}
    username: ${DB_USERNAME:${POSTGRES_USER:admin}}
    password: ${DB_PASSWORD:${POSTGRES_PASSWORD:admin}}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  jackson:
    default-property-inclusion: non_null

toolshare:
  security:
    jwt-secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
  services:
    tool-url: ${TOOL_SERVICE_URL:http://localhost:8083}
>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)

management:
  endpoints:
    web:
      exposure:
        include: health,info
<<<<<<< HEAD

instance:
  prefer-ip-address: true
=======
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.toolshare.booking: INFO

>>>>>>> 352f75b (fetch: service for ai-service add and auth service update & toolservice update)
